[project]
name = "monitoring"
version = "0.1.0"
description = "Monorepo: monitor-service + monitor-sdk"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["monitor-service", "sdk"]

[dependency-groups]
dev = [
    "build>=1.2.0,<2.0.0",
    "ruff>=0.6.0",
]
